name: fiap_dashboard_unified
description: Unified dashboard for FIAP AI-Enhanced Learning Platform MVP
version: 1.0.0
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # State Management
  flutter_riverpod: ^3.0.3
  
  # HTTP Client
  dio: ^5.4.0
  
  # Routing
  go_router: ^14.0.0
  
  # Firebase
  firebase_core: ^4.2.1
  firebase_auth: ^6.1.2
  
  # UI
  flutter_svg: ^2.0.9
  intl: ^0.18.1
  
  # Storage
  shared_preferences: ^2.2.2
  
  # Local packages - path dependencies to implemented packages
  tema:
    path: ../../packages_dashboard/tema
  dashboard_auth:
    path: ../../packages_dashboard/dashboard_auth
  approval_interface:
    path: ../../packages_dashboard/approval_interface
  research_dashboard:
    path: ../../packages_dashboard/research_dashboard

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  mockito: ^5.4.4
  build_runner: ^2.4.7

flutter:
  uses-material-design: true
  
  # Assets
  assets:
    - assets/images/
    - assets/icons/

# Override incompatible versions from path dependencies
dependency_overrides:
  flutter_riverpod: ^3.0.3
  riverpod: ^3.0.3
